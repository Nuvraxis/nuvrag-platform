# Where the dashboard's server side reaches the FastAPI service. Read at runtime, never
# exposed to the browser: every API call is made from the Next.js server so the access token
# can stay in an httpOnly cookie.
API_BASE_URL=http://localhost:8000
